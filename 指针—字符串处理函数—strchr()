//#include <string.h>
//char* strchr(const char* s, int c);
////功能：在字符串s中查找字母c出现的位置
////参数：
////		s：字符串首地址
////		c：匹配字母(字符)
////返回值：
////		成功：返回第一次出现的c地址
////		失败：NULL
#include<stdio.h>
int main() {
	char src[] = "ddda123abcd";
	char* p = strchr(src, 'a');
	printf("p = %s\n", p);
}
